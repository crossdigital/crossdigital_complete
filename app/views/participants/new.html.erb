<%= form_with(model: [@game, @participant]) do |form| %>
  <% if @participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% @participant.errors.each do |error| %>
          <li><%= @participant.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.select :role, options_for_select(Participant::ROLES) %>
  </div>

  <div class="field">
    <%= form.label :team %>
    <%= form.select :team, options_for_select(Participant::TEAMS) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
